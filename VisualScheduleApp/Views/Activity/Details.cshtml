@model VisualScheduleApp.Models.Activities.ActivityViewModel

<h1>Tegevuse detailid</h1>

<div>
    <dl class="row">
        <div class="form-group row">
            <label asp-for="Id" class="col-sm-2 col-form-label">ID</label>
            @Html.DisplayFor(model => model.Id)
        </div>
    </dl>

    <dl class="row">
        <div class="form-group row">
            <label asp-for="Name" class="col-sm-2 col-form-label">Nimi</label>
            @Html.DisplayFor(model => model.Name)
        </div>
    </dl>

    <dl class="row">
        <div class="form-group row">
            <label asp-for="Description" class="col-sm-2 col-form-label">Kirjeldus</label>
            @Html.DisplayFor(model => model.Description)
        </div>
    </dl>

    <dl class="row">
        <div class="form-group row">
            <label asp-for="CreatedAt" class="col-sm-2 col-form-label">Loodud</label>
            @Html.DisplayFor(model => model.CreatedAt)
        </div>
    </dl>

    <dl class="row">
        <div class="form-group row">
            <label asp-for="ModifiedAt" class="col-sm-2 col-form-label">Muudetud</label>
            @Html.DisplayFor(model => model.ModifiedAt)
        </div>
    </dl>

    @foreach (var images in Model.Image)
    {
        <partial name="_Images" model="images" />
    }

    <hr />
    <a asp-action="Index" asp-controller="Activity" class="btn btn-danger">Tagasi</a>
</div>