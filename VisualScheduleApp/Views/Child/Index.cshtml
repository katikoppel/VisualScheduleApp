@model IEnumerable<VisualScheduleApp.Models.Children.ChildViewModel>

@{
    ViewData["Title"] = "Children";
}

<h1>Lapsed</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Lisa uus</a>
</p>

<table class="table table-hover">
    <thead>
        <tr>
            <th>Nimi</th>
            <th>Sünniaeg</th>
            <th>Vanus</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var child in Model)
        {
            <tr>
                <td>@child.Name</td>
                <td>@child.BirthDate.ToShortDateString()</td>
                <td>@child.Age</td>
                <td>
                    <a asp-action="Details" asp-route-id="@child.Id" class="btn btn-success btn-sm">Detailid</a>
                    <a asp-action="Update" asp-route-id="@child.Id" class="btn btn-primary btn-sm">Uuenda</a>
                    <a asp-action="Delete" asp-route-id="@child.Id" class="btn btn-danger btn-sm">Kustuta</a>
                </td>
            </tr>
        }
    </tbody>
</table>